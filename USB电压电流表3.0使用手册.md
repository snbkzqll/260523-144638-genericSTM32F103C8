# USB 电压电流表 3.2 (Cyber-Dashboard + 原版精准采样) 使用手册

本说明书面向 USB 电压电流表的维护者与使用者，详细说明固件 3.2 版本的界面布局、按键交互、AT24C EEPROM 高频无感自动保存及波形切换功能。

---

## 1. 功能概述

* **100% 恢复硬件主回路精准采样**：锁定硬件大电流采样回路（`M1=HIGH, M2=LOW`），恢复原工程 `PA1` 采样算法（`aRaw / 101 / 5`），彻底解决电流偏小、读数卡顿问题。
* **AT24C EEPROM 5秒全自动高频保存**：已激活板载 AT24C 记忆芯片，后台每隔 5 秒全自动分页无感保存，拔电不丢数据。
* **5 大极客视图 (Cyber Views)**：包含主仪表盘、快充/网格、示波器波形、能量极值统计与 HUD 系统设置菜单。
* **4 组独立数据组 (Group 0~3)**：可针对不同充电设备独立记录容量 ($mAh$)、能量 ($mWh$)、运行时间及电压/电流/功率极值。
* **示波器波形源切换**：支持短按 K4 切换示波器绘制【电压波动曲线】或【电流动态曲线】。

---

## 2. 按键操作说明

| 按键引脚 | 标示名称 | 触发方式 | 操作功能 |
| :--- | :--- | :--- | :--- |
| **PA15** | `K1` | **短按** | **切页 (PAGE)**：在 5 个显示界面间循环切换 |
| **PB4**  | `K2` | **短按** | **切组 (GROUP)**：循环切换激活的数据组 (Group 0 $\rightarrow$ 1 $\rightarrow$ 2 $\rightarrow$ 3) |
| | | **长按 (>1.5s)** | **清零 (CLEAR)**：清零当前激活组的 $mAh$、$mWh$、运行时间与极值记录 |
| **PB10** | `K3` | **短按** | **旋转 (ROTATE)**：屏幕 180° 上下翻转（适配不同插入方向） |
| | | **长按 (>1.5s)** | **保存 (SAVE)**：手动触发全量保存 (正常使用无需手动长按，系统已 5s 自动保存) |
| **PB1**  | `K4` | **短按** | **波形源切换**：切换 Page 2 示波器绘制【电压曲线 `V-WAVE`】或【电流曲线 `A-WAVE`】 |

---

## 3. 5 大显示视图指南

### 1️⃣ Page 0: 赛博主仪表盘 (Main Overview)
* **顶部状态栏**：`[G0]` 数据组徽标、`●` 动态心跳指示点、`LIVE` 状态及实时 NTC 温度。
* **主区域**：左侧 `VOLT` 卡片（超大字总电压 $V$），右侧 `CURR` 卡片（超大字电流 $A$）。
* **底部栏**：实时功率 $P (W)$ 与累计容量 $mAh$。

### 2️⃣ Page 1: 快充协议与网格 (Details & Protocol)
* **4 宫格卡片**：精确输入电压 $V$、电流 $A$、功率 $W$ 以及负载等效电阻 $R (\Omega)$。
* **协议卡片**：反色高亮显示识别出的快充协议徽标（如 `[ QC3.0 ]`），右侧显示 $D+$ 与 $D-$ 精细电压。

### 3️⃣ Page 2: 示波器趋势波形 View (Scope)
* **实时折线图**：绘制最近 60 秒的电压或电流动态波形（按 K4 切换源）。
* **极值标尺**：右侧展示波形的最高值 (`H:`)。

### 4️⃣ Page 3: 能量统计与极值仪表 (Energy Analytics)
* **能量卡片**：大字展示当前数据组的 $mAh$ 毫安时容量与 $mWh$ 能量。
* **极值记录**：捕捉最高电压 $V_{MAX}$ 与最高电流 $I_{MAX}$。
* **计时器**：充电持续时间 $HH:MM:SS$。

### 5️⃣ Page 4: HUD 系统设置菜单 (Settings)
* 显示当前视图、存储引擎状态（显示 `AT24C EEPROM`）、波形源状态，以及按键提示。
