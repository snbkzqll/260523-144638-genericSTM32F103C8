# USB 电压电流表 3.8 (电流精准修复 + 两点电压线性校准) 使用手册

本说明书面向 USB 电压电流表的维护者与使用者，详细说明固件 3.8 版本的界面布局、原版精准电流采样、两点线性电压校准、按键极速连发、智能自动熄屏及数据线线阻测试指南。

---

## 1. 核心升级与修复说明

1. **彻底纠正电流计算公式（恢复 0.45A 精准测量）**：
   * 修复了此前在缩放运算中因分母重复计算导致电流翻倍显示为 0.96A 的问题。
   * 电流采样算法 100% 严格回归原作者经过硬件增益标定的物理公式：
     $$I (\text{A}) = \frac{\text{PA1 电压 (mV)}}{505.0}$$
   * 0.45A 电流实测显示绝对精准，无任何倍数失真。
2. **两点线性电压校准 (`V-Scale` 斜率 + `V-Off` 零点偏置)**：
   * 彻底解决 3.3V/Micro 供电二极管压降（0.3V）与 5V Type-C 供电的矛盾。
   * 在 Page 4 设置页中：
     * **`V-Scale` (电压斜率，默认 12.01)**：在 5V Type-C 供电下调准分压比；
     * **`V-Off` (电压零点补偿，默认 0.00V)**：如果使用 3.3V / Micro 供电，直接按键将 `V-Off` 调至 `+0.30V`，3.3V 和 5V 同时 100% 精准！
3. **按键长按极速连发**：
   * 在 Page 4 微调任何校准参数时，按住 K2 [-] 或 K3 [+] 超过 400ms，自动开启每秒 15 次的极速连发连续加减。
4. **智能无动作自动熄屏与一键唤醒**：
   * 支持自由设置 `1min` / `2min` / `3min` / `5min` / `10min` / `OFF`，超时自动关闭 OLED 驱动芯片，防烧屏并省电；按任意键一秒唤醒。
5. **双重物理存储备份**：
   * 自动同步双写至 AT24C EEPROM 与内部 Flash，断电绝对永久保留。

---

## 2. 5 大视图指南与按键操作

### 🔘 普通视图下的按键操作 (Page 0 ~ Page 3)
* **K1 (PA15)**：【短按】切页 (Page 0 $\rightarrow$ 1 $\rightarrow$ 2 $\rightarrow$ 3 $\rightarrow$ 4)
* **K2 (PB4)**：【短按】切换数据组 (G0 $\rightarrow$ G1 $\rightarrow$ G2 $\rightarrow$ G3)；【长按>1.5s】**清零当前数据组**
* **K3 (PB10)**：【短按】屏幕 180° 旋转；【长按>1.5s】手动触发一次全量保存
* **K4 (PB1)**：【短按】切换 Page 2 示波器波形源（电压曲线 `V-WAVE` $\leftrightarrow$ 电流曲线 `A-WAVE`）
* **【任意键】**：息屏状态下按任意键秒级唤醒屏幕。

---

### ⚙️ Page 4：极客设置与校准菜单操作指南

短按 **K1 键** 切换到 **Page 4**：

* **K4 键 (PB1)**：【短按】**移动光标 `>`**，在 6 个设置项之间循环切换：
  1. `> Bright:  XX%` (屏幕亮度，短按 K2 减 / K3 增)
  2. `> V-Scale: XX.XX` (电压斜率，**标准默认 12.01**，支持长按连发)
  3. `> V-Off:   +X.XXV` (电压零点补偿，**可调 +0.00 ~ +0.50V 吸收二极管压降**，支持长按连发)
  4. `> I-Cal:   XXX` (电流分母系数，**标准默认 505**，支持长按连发)
  5. `> Sleep:   2min` (自动熄屏时间，短按选择 `1min`/`2min`/`3min`/`5min`/`10min`/`OFF`)
  6. `> CableR:  XXXmR` (数据线内阻测试)
