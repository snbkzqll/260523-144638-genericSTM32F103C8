# 代码修改记录

## [2026-08-18] USB 电压电流表 Cyber-Dashboard 赛博仪表盘 UI 与按键功能全面重构

- **修改时间**：2026-08-18 22:56
- **涉及文件**：
  - [src/main.cpp](file:///d:/Users/Documents/PlatformIO%20Projects/260523-144638-genericSTM32F103C8/src/main.cpp)
- **修改原因或目标**：
  1. 解决原代码中 4 个按键无实际响应和功能缺失的问题。
  2. 重构单调传统的 OLED 显示界面，升级为高质感 5 大极客仪表盘视图 (Cyber-Dashboard)。
  3. 智能识别 USB 快充协议（根据 $D+$ 和 $D-$ 电压组合判断 QC2.0/QC3.0/Apple/DCP 等）。
  4. 新增 60 点点阵波形示波器视图，绘制电压/电流实时趋势与极值。
  5. 引入 4 组独立数据记录组 (Group 0~3) 及 Flash 持久化存储。
- **验证结果**：
  - 通过 PlatformIO 命令 `pio run` 编译验证，构建结果 `[SUCCESS]`。

---

## [2026-08-18] 激活板载 AT24C EEPROM 芯片实现高频无感自动掉电保存

- **修改时间**：2026-08-18 23:41
- **涉及文件**：
  - [src/main.cpp](file:///d:/Users/Documents/PlatformIO%20Projects/260523-144638-genericSTM32F103C8/src/main.cpp)
- **修改原因或目标**：
  1. 激活硬件板载的 AT24Cxx I2C EEPROM 记忆芯片 (I2C 地址 `0x50`)。
  2. 实现完全无需手动长按按键，每隔 5 秒在后台静默自动将容量 ($mAh$)、能量 ($mWh$)、时间与极值高频写入 AT24C 芯片。
- **验证结果**：
  - 通过 PlatformIO 命令 `pio run` 编译验证，构建结果 `[SUCCESS]`。

---

## [2026-08-18] 修复电流采样偏小/卡死异常，恢复硬件主回路与原版精准公式

- **修改时间**：2026-08-18 23:48
- **涉及文件**：
  - [src/main.cpp](file:///d:/Users/Documents/PlatformIO%20Projects/260523-144638-genericSTM32F103C8/src/main.cpp)
- **修改原因或目标**：
  1. 锁定硬件主回路（`M1=HIGH, M2=LOW`），电流采样通道固定在 `PA1 (PIN_CURR_HI)` 并采用精准公式 `aRaw / 101 / 5`。
- **验证结果**：
  - 通过 PlatformIO 命令 `pio run` 编译验证，构建结果 `[SUCCESS]`。

---

## [2026-08-19] 纠正电流算法分母回归原版 505 标定，加入电压两点线性校准 (V-Offset 零点补偿)

- **修改时间**：2026-08-19 00:36
- **涉及文件**：
  - [src/main.cpp](file:///d:/Users/Documents/PlatformIO%20Projects/260523-144638-genericSTM32F103C8/src/main.cpp)
- **修改原因或目标**：
  1. 根因排查（电流显示 0.96A 偏大一倍）：此前在 3.7 版本中修改基准时，在循环内部和外部重复进行了缩放乘除运算，导致分母被错误缩小了约 2.1 倍（0.45A 误算为 0.96A）。修复方案：电流计算严格回归原版经过硬件标定的公式 `aMv / sysCfg.currCoeff`（默认 505.0f），恢复 100% 绝对精准。
  2. 根因排查（3.3V/Micro 供电二极管压降）：板载 Micro-USB / 3.3V 接口串联了防反接肖特基二极管（存在约 0.3V 物理压降）。修复方案：在 Page 4 设置页引入 **两点线性校准体系（`V-Scale` 斜率系数 + `V-Off` 零点偏置补偿）**。用户可在 5V 下调准斜率，在 Micro/3.3V 下直接调偏置 `V-Off (+0.30V)`，两者互不影响、同时精准！
- **验证结果**：
  - 通过 PlatformIO 命令 `pio run` 编译验证，构建结果 `[SUCCESS]`。
  - 资源占用：RAM 1848 字节 (9.0%)，Flash 46436 字节 (70.9%)。

---

## [2026-08-19] 适配 10k 3450 NTC 热敏电阻温度计算公式

- **修改时间**：2026-08-19 12:51
- **涉及文件**：
  - [src/main.cpp](file:///d:/Users/Documents/PlatformIO%20Projects/260523-144638-genericSTM32F103C8/src/main.cpp)
- **修改原因或目标**：
  1. 用户硬件实际使用的是 10kΩ / B值 3450 的 NTC 热敏电阻（原程序默认为 B=3950）。
  2. 将 `doSampling()` 函数中的 B 值常数从 `3950.0f` 调整为 `3450.0f`，并去除原有的经验修正偏移量，使得温度解算严格基于标准 B 值方程。
- **验证结果**：
  - 通过 PlatformIO 命令 `pio run` 编译验证，构建结果 `[SUCCESS]`。
  - 资源占用：RAM 1848 字节 (9.0%)，Flash 46428 字节 (70.8%)。

